/**
 * Постоянные заголовков клиента (§1). Отдельным модулем, потому что их читает
 * и middleware, а туда нельзя тянуть next/headers вместе со всем клиентом.
 */

/** Версия сайта. Показывается в списке устройств (§4.4) строкой сессии. */
export const APP_VERSION = process.env.NEXT_PUBLIC_APP_VERSION ?? '0.1.0 (web)';

/** X-Client-Platform: приложение шлёт ios, сайт — web. */
export const CLIENT_PLATFORM = 'web';

/**
 * Идентификатор «устройства» самого сайта.
 *
 * Нужен запросам, у которых нет человека: правовые документы читает сервер при
 * пересборке страницы, и брать идентификатор из cookie там неоткуда — да и
 * незачем. Постоянное значение, а не случайное на каждый запрос: по нему
 * считается лимит на чтение документов (§15.1), и случайный сделал бы лимит
 * бессмысленным.
 *
 * Задаётся SITE_DEVICE_ID, если у стенда и прода должны быть разные счётчики.
 */
export const SITE_DEVICE_ID =
  process.env.SITE_DEVICE_ID ?? '00000000-0000-4000-8000-000000000001';
